{
  "version": 3,
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node",
      "config": { "tsConfig": "tsconfig.json" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.ts"
    }
  ]
}
